info.app.name=Very Cool App
info.app.description=A very cool app indeed!
info.app.version=0.0.1-alpha-preview

# Expose all endpoints
# Uh, don't do this in a live application.
management.endpoints.web.exposure.include=*

# Exclude some endpoints
management.endpoints.web.exposure.exclude=health,info

# Set spring security username and password
spring.security.user.name=terminalnode
spring.security.user.password=springboot